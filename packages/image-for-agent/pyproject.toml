[project]
name = "image-for-agent"
version = "0.1.0"
description = "CLI that prepares local images for manual agent input, plus an offline image variant benchmark harness."
authors = [{ name = "Lewis Howles" }]
license = { text = "MIT" }
requires-python = ">=3.11"
dependencies = ["Pillow>=10.0"]
classifiers = [
	"Development Status :: 3 - Alpha",
	"Intended Audience :: Developers",
	"License :: OSI Approved :: MIT License",
	"Programming Language :: Python :: 3",
	"Programming Language :: Python :: 3.11",
	"Topic :: Multimedia :: Graphics",
]

[project.urls]
Homepage = "https://github.com/lewishowles/dev-tools/tree/main/packages/image-for-agent"
Repository = "https://github.com/lewishowles/dev-tools"

[project.scripts]
image-for-agent = "image_for_agent.public_cli:main"
image-for-agent-benchmark = "image_for_agent.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/image_for_agent"]

[tool.pytest.ini_options]
pythonpath = ["src"]
